# @boske-labs/grove-fit — core

Pure TypeScript fit engine (browser + Node).

**Status:** implemented — parity tests vs Boske `hardware-fit.test.js`.

## Exports

- `assignMaxTier`
- `buildHardwareFitSnapshot`
- `computeTierFitLevel`
- `suggestBoskeTierForParams`
- `buildFunnelComparison`

## Tests

```bash
bun test
```
